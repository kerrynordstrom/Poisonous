{
  "name": "poisono-us",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node ./server/app-hooked.js",
    "postinstall": "npm install --prefix server && npm install --prefix poison-client"
  },
  "engines": {
    "node": "~6.10.3",
    "npm": "~5.6.0"
  }
}
